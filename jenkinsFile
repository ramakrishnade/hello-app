@Library("Jenkins-pipeline-library") _

pipeline {
    agent any
    stages {
        stage('Code Checkout') {
            steps {
               script {
                  sayHello()
                }
            }
        }
    }
}
