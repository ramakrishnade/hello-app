@Library("Jenkins-pipeline-library") _

pipeline {
    agent any
    stages {
        stage('Code Checkout') {
            steps {
                script {
                  def workingDir = checkoutScm("https://github.com/ramakrishnade/hello-app.git", "master")
                  echo $workingDir
                }
            }
        }
    }
}
